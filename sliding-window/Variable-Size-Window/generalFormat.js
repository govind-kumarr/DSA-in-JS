function variableSizeWindow(arr, N, cond) {
  let i = 0,
    j = 0;
  while (j < N) {
    //Calculations
    if (cond < k) j++;
    else if (cond == k) {
      //! Deduce Answer from the calculations
      j++;
    } else if (cond > k) {
      while (cond > k) {
        //** Remove the Previous Calculations */
      }
      j++
    }
  }
}
